/**
 * 선물의 규칙 중 화면도 알아야 하는 것. 진짜 판정은 서버(RLS·트리거)가 하고,
 * 여기 값은 그 정책을 도움말이 옮겨 적기 위한 사본이다 —
 * 서버 정책을 바꾸면 이 수도 함께 고칠 것.
 *
 * 이 파일에 import가 없는 것은 우연이 아니다. 도움말이 읽어야 하는데,
 * supabase를 끌어오는 모듈에 두면 혼자 하는 실행에도 서버 코드가 딸려 온다.
 */
export const GIFT_DAILY_LIMIT = 10
